# Railway uses Nixpacks by default. PocketBase may shell out to `ps` on Linux;
# minimal images often omit it → "spawn ps ENOENT". `unzip` is required by setup.js.
[phases.setup]
aptPkgs = ["...", "procps", "unzip"]
